name: API Unit Tests - 300 API Test Suite

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-api-unit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install root dependencies
        run: npm ci || npm install

      - name: Start Web App & Backend API in background on port 8081
        run: |
          npx vite --port 8081 &
          echo "Waiting for app server on http://localhost:8081..."
          until curl -s http://localhost:8081 > /dev/null; do
            sleep 2
          done
          echo "App server is ready on http://localhost:8081!"

      - name: Install API Unit test suite dependencies
        working-directory: ./collabroom-tests/api-unit
        run: npm ci || npm install

      - name: Run all 300 API Unit tests
        working-directory: ./collabroom-tests/api-unit
        run: npm test

      - name: Generate 300-Row API Unit Excel report
        working-directory: ./collabroom-tests/api-unit
        run: npx ts-node generate-report.ts

      - name: Upload API Unit 300 Test Report artifact
        uses: actions/upload-artifact@v4
        with:
          name: api-unit-report-300.xlsx
          path: collabroom-tests/reports/api-unit*.xlsx
